[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "qcs-oracle-automation"
version = "0.1.0"
description = "AI-recorded, AI-free replay test automation for Oracle EBS (Java agent + Playwright)"
requires-python = ">=3.11"
dependencies = [
    # Browser automation
    "playwright>=1.44",
    # Local Java Forms agent is built from java-agent/.
    # MCP server
    "fastmcp>=0.1",
    # Excel I/O
    "openpyxl>=3.1",
    "pandas>=2.2",
    # YAML
    "pyyaml>=6.0",
    # Jinja2 templates (codegen)
    "jinja2>=3.1",
    # Screenshot / Windows / UI automation
    "pillow>=10.0",
    "pywin32>=306",
    "pyautogui>=0.9",
    # HTTP
    "aiohttp>=3.9",
    # Embeddings / FAISS (form fingerprint similarity search)
    "sentence-transformers>=3.0",
    "faiss-cpu>=1.8",
    # Azure monitoring (telemetry)
    "azure-monitor-opentelemetry>=1.6",
    "opentelemetry-api>=1.25",
    # Pytest ecosystem
    "pytest>=8.2",
    "pytest-asyncio>=0.23",
    "pytest-html>=4.1",
    "pytest-xdist>=3.5",
]

[project.optional-dependencies]
dev = ["ruff", "mypy"]
center = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.29",
    "aiosqlite>=0.20",
]
agent = [
    "httpx>=0.27",
]

[project.scripts]
qcs        = "qcs.__main__:main"
qcs-center = "qcs_center.app:main"
qcs-agent  = "qcs_agent.main:main"
qcs-studio = "qcs_studio.app:main"

[tool.setuptools.packages.find]
where = ["."]
include = ["qcs*", "qcs_repo*", "qcs_replay*", "qcs_java_agent*", "generator*", "oracle_ai_agent*", "qcs_center*", "qcs_agent*", "qcs_studio*"]

[tool.pytest.ini_options]
markers = [
    "oracle: marks tests that run against Oracle EBS",
    "ebs: marks tests that exercise Oracle EBS business flows",
    "generated: marks auto-generated replay scripts",
    "browser: marks tests that require a Playwright browser session",
    "java_forms: marks tests that interact with Oracle Java Forms",
]
asyncio_mode = "auto"
testpaths = ["tests"]
pythonpath = ["."]

[tool.setuptools]
py-modules = ["config"]

[tool.ruff]
line-length = 100
target-version = "py311"
