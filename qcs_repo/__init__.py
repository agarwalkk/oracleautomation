"""
qcs_repo — Object Repository library.

Provides:
  store.py       — YAML read/write for forms, elements, flows, index
  identity.py    — stable screen/element refs, uids, and locator candidates
  fingerprint.py — compute a stable form_id from a Java-agent or HTML snapshot
  naming.py      — AI-propose + human-confirm friendly name flow
  snapshot.py    — shared snapshot text-encoder (reused by healer)
"""
